/**
 * "Open Beads Manager" entry point: calls `manager.ensure` for the current
 * workspace and opens the returned agent.
 *
 * Empty in WP-108 by design — the behaviour is the deliverable of WP-113.
 * Client-side rules for this file: React Native primitives only, colors from
 * theme.colors, and no Node builtin imports.
 */
export {};
